import { Panel } from "@/ui";
/* Stub — the Barndominium Builder is being built on the shared 5D core. */
export default function Barndo() {
  return (
    <Panel className="grid min-h-[240px] place-items-center">
      <span className="lab">The Barndominium Builder is coming online.</span>
    </Panel>
  );
}
