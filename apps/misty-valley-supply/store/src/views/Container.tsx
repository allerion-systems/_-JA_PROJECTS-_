import { Panel } from "@/ui";
/* Stub — the Container Designer is being built on the shared 5D core. */
export default function Container() {
  return (
    <Panel className="grid min-h-[240px] place-items-center">
      <span className="lab">The Container Designer is coming online.</span>
    </Panel>
  );
}
