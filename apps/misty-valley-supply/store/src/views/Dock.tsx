import { Panel } from "@/ui";
/* Stub — the Dock Designer is being built on the shared 5D core. */
export default function Dock() {
  return (
    <Panel className="grid min-h-[240px] place-items-center">
      <span className="lab">The Dock Designer is coming online.</span>
    </Panel>
  );
}
