import { Panel } from "@/ui";
/* Stub — the Program tool is being built. */
export default function Program() {
  return (
    <Panel className="grid min-h-[240px] place-items-center">
      <span className="lab">The Program tool is coming online.</span>
    </Panel>
  );
}
