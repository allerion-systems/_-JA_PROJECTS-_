import { Panel } from "@/ui";
/* Stub — the Warehouse tool is being built. */
export default function Warehouse() {
  return (
    <Panel className="grid min-h-[240px] place-items-center">
      <span className="lab">The Warehouse tool is coming online.</span>
    </Panel>
  );
}
